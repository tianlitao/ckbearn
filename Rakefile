# Add your own tasks1 in files placed in lib/tasks1 ending in .rake,
# for example lib/tasks1/capistrano.rake, and they will automatically be available to Rake.

require_relative "config/application"

Rails.application.load_tasks
